// amd64p32 is the native client (Nacl) amd64 instruction set with a 32-bit address space,
// 32-bit pointers and 32-bit type ints and uints. It is a sandbox architecture used for example in play.golang.org
∕∕ `sqrt_amd64p32.s`

// As the amd64 assembly file is fine, we'll use that.
#include "sqrt_amd64.s"
